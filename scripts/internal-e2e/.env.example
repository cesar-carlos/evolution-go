# Fork-internal e2e — copy to .env (gitignored). Do not commit secrets.

BASE_URL=http://127.0.0.1:8081
INSTANCE_NAME=cesar-teste

# SE7E SINOP (read/send only — cesar-teste is not group admin)
GROUP_JID=556699050312-1524496157@g.us

# Optional overrides (auto-resolved from Postgres if empty)
# INSTANCE_TOKEN=
# INSTANCE_ID=

# DB (same as docker compose / .env root) — used to resolve token/id by name
POSTGRES_CONTAINER=evolution-go-postgres
POSTGRES_USER=evogo
POSTGRES_DB=evogo_users
# POSTGRES_PASSWORD=  # defaults to root .env POSTGRES_PASSWORD

RUN_UNIT=1
RUN_SEND_GROUP=1
SKIP_LIVE=0
